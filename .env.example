# 백엔드(NestJS) 주소 — 프론트 서버 컴포넌트가 이 백엔드를 호출합니다.
CYPHERS_API_URL=http://localhost:4000/api

# (선택) 개발용 /api/debug 라우트에서만 사용됩니다.
# 실제 Neople API 키는 이제 backend/.env 에 둡니다 (백엔드가 프록시/캐싱 담당).
NEOPLE_API_KEY=
